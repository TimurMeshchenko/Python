Пароль у акков fsafsafzcx

1. закладки

Добавить к пользователю аттрибут закладок.
В закладках получать user и выводить все его тайлты

Создать модель int user id ManyToManyField(Title) 
Title.objects.get(dir_name="{self.dir_name}").chapters.add(Chapters.objects.get_or_create(chapter="{chapter}", tome={tome})[0])
f'Bookmarks.objects.get_or_create(users=user).titles.add(Title.objects.get(dir_name="{dir_name}")) dir_name можно получить из url

Сделать модель закладок id будет как у user id 
ManyToManyField(Title) (чтобы загружать добавленные тайтлы) 

Добавить в закладки. post в таблицу bookmarks созд запись пользователь id тайтла
Удалять закладки

2. Лайки и т.д

при нажатии отправляется post запрос который +1 лайк в бд для этого тайтла 
сохраняю user id который поставил, если еще раз нажимает -1 и удаляю из лайков user id

3. Комментарии

post в модель комментарии с textfield для каждого тайтла

4. Добавить 10 страниц в каталоге
спарсить добавить url для страниц

fastapi + redis + restapi asyncio, закинуть в docker, 
изучить команды mongodb PostgreSQL

from remanga.models import *

